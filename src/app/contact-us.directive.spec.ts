import { ContactUsDirective } from './contact-us.directive';

describe('ContactUsDirective', () => {
  it('should create an instance', () => {
    const directive = new ContactUsDirective();
    expect(directive).toBeTruthy();
  });
});
